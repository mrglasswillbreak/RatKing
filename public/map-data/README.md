# LASU Ojo PMTiles placeholder

Place the production LASU Ojo campus PMTiles extract at `public/map-data/lasu-ojo.pmtiles`.
The app is wired for `pmtiles:///map-data/lasu-ojo.pmtiles` so the same URL is cached by Serwist and can be mirrored into IndexedDB by the offline tile cache layer.
